<UserControl x:Class="WpfControls.MyTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Margin="0,0,0,0"  VerticalScrollBarVisibility="Visible">
            <TreeView x:Name="mytreeview" HorizontalAlignment="Stretch"  Margin="0,0,0,0" VerticalAlignment="Stretch"/>
        </ScrollViewer>
        
    </Grid>
</UserControl>
